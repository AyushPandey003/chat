name: Keep Render Site Awake

on:
  schedule:
    - cron: '*/50 * * * *'  
  workflow_dispatch:        

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping your Render site
        run: curl -m 10 https://chat-wan8.onrender.com/
